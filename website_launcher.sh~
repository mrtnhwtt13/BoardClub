#!/bin/bash
# Repository Automate
mail=sebastien.choubrac@epitech.eu
echo "Name of repository ?"
read repo
blih -u $mail repository create $repo
blih -u $mail repository setacl $repo ramassage-tek r
blih -u $mail repository setacl $repo gecko r
git clone git@git.epitech.eu:/$mail/$repo

